require "rails_helper"

RSpec.feature "Users adding books" do
  context "when authenticated" do
    scenario "a user can add a book" do
      # As an authenticated user
      # when I visit the form to add a book
      # I fill in the books title
      # and I fill in the books price
      # and I fill in the books quantity
      # and I click on "Create Book"
      # Then I should be on the books index page
      # and I should see the books title - price - quantity


    end
  end
end
